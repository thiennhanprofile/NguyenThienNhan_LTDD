import { StyleSheet } from "react-native";
const st = StyleSheet.create({
  khungDSSP: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
  },
  itemPro: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
  },
});
